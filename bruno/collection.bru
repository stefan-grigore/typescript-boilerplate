meta {
  name: TS Boilerplate — Fastify + OAuth2
}

auth {
  mode: none
}

docs {
  Collection to test the Fastify + Zod + OAuth2 boilerplate.
  Import this into Bruno (it can import Postman v2.1).
}
